#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
	
	// Declarando as variáveis
	int numeros[5];
	
	// Atribuindo os valores dos arrays
	numeros[0] = 1;
	numeros[1] = 2;
	numeros[2] = 3;
	numeros[3] = 4;
	numeros[4] = 5;
	
	// Exibindo o resultado no terminal
	printf("Numeros: %d\n", numeros[0]);
	printf("Numeros: %d\n", numeros[1]);
	printf("Numeros: %d\n", numeros[2]);
	printf("Numeros: %d\n", numeros[3]);
	printf("Numeros: %d\n", numeros[4]);
	
}

