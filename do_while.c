#include <stdio.h>

int main(){
	// Declarando as variáveis
	int i = 11;

	// Faz com que o bloco de comandos seja executado pelo menos uma vez de forma obrigatioria
	do{
		printf("%d ", i);
		i++;
	// Prosseguindo com o programa
	}while(i <= 10);
	
}
